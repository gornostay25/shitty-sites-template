---
/**
 * i18n stub — uncomment and wire when adding locales in astro.config.mjs.
 *
 * 1. Add locales: ["en", "uk"], fallback: { uk: "en" }
 * 2. NEVER set prefixDefaultLocale: true — breaks /_emdash/admin
 * 3. Import and render this component in SiteHeader.astro
 *
 * Docs: https://docs.emdashcms.com/guides/internationalization/
 */
// import { getHreflangAlternates } from "emdash";
---

<!-- LanguageSwitcher disabled — English only in astro.config.mjs -->
