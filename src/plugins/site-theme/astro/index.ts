/**
 * Portable Text block map — add entries here when forking.
 * No React islands in this map (Astro ignores client:* on PT components).
 */
export const blockComponents = {
	// "site-theme.example": ExampleBlock,
};
