/**
 * Stub admin page — replace with Kumo form primitives + apiFetch() pattern.
 * Docs: https://docs.emdashcms.com/plugins/creating-native-plugins/admin-ui/
 */
export default function SiteThemeAdminPage() {
	return (
		<div>
			<h1>Site theme</h1>
			<p>Replace with plugin settings UI (Kumo + apiFetch unwrap).</p>
		</div>
	);
}
